# cloud-scanner/ — reserved, not functional

This directory is a skeleton reserved for the planned Go scanning engine (see
[ROADMAP.md](../ROADMAP.md), "Further out"). It currently does not compile (no
`go.sum` committed, unresolved import cycle) and is not built by CI, shipped, or
part of what Kaaval does today. The shipped product lives in
[`control-plane/`](../control-plane/) and [`dashboard/`](../dashboard/).
