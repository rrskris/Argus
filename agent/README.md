# agent/ — reserved, not functional

This directory is a skeleton reserved for the planned Go runtime agent (see
[ROADMAP.md](../ROADMAP.md), "Further out"). It is not built by CI, not shipped
in any image, and not part of what Kaaval does today. The shipped product lives
in [`control-plane/`](../control-plane/) and [`dashboard/`](../dashboard/).
